<body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container-fluid">
            <b class="navbar-brand">Pinball High Score</b>
            
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
              <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNavDropdown">
                <ul class="navbar-nav">
                    <% if (typeof adminid !== 'undefined') { %>
                        <li class="nav-item"><a class="nav-link" aria-current="page"  href="/machines">Machines</a></li>
                        <li class="nav-item"><a class="nav-link" aria-current="page"  href="/players">Players</a></li>
                        <li class="nav-item"><a class="nav-link" aria-current="page"  href="competition_admin.ejs">Competition</a></li>
                    <% } %>

                    <% if (typeof userid !== 'undefined') { %>
                    <li class="nav-item"><a class="nav-link" aria-current="page"  href="/addscore">Add Score</a></li>
                    <li class="nav-item"><a class="nav-link" aria-current="page"  href="competition_player.ejs">Competition</a></li>
                    <% } %>

                    <% if (typeof userid !== 'undefined' || typeof adminid !== 'undefined') { %>
                    <li class="nav-item"><a class="nav-link" aria-current="page"  href="/logout">Log out</a></li>
                    <% } %>

                    <% if (typeof userid === 'undefined' && typeof adminid === 'undefined') { %>
                    <li class="nav-item"><a class="nav-link" aria-current="page"  href="/competition">Competition</a></li>
                    <li class="nav-item"><a class="nav-link" aria-current="page"  href="/">Log in</a></li>
                    <% } %>
                </ul>
            </div>
        </div>
   </nav>
   <% if (typeof error !== 'undefined') { %>
    <div class="alert alert-danger"><%= error %></div>
    <% } %>
